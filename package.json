{
  "name": "tickets",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "dotenv -- cross-var skaffold --default-repo=%GCLOUD_PROJECT_REPO% dev"
  },
  "devDependencies": {
    "cross-var": "^1.1.0",
    "dotenv": "^8.2.0",
    "dotenv-cli": "^4.0.0"
  }
}
